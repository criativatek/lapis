/.phpunit.cache
/.superpowers
/bootstrap/ssr
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/resources/js/actions
/resources/js/routes
/resources/js/wayfinder
/vendor
.DS_Store
.env
.env.backup
.env.production
# The local testing environment: its own APP_KEY and its own database
# credentials. The shape that is safe to share lives in .env.testing.example.
.env.testing
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed

# Claude Code local tooling (skills marketplace cache, not project source)
/.agents
/.claude/skills
/.claude/settings.json
/skills-lock.json
/AGENTS.md.local-backup-*

# Real, non-anonymized student export samples used only to design the
# roster-import feature (docs/superpowers/specs/2026-07-28-*) — never commit.
/Ficheiros avulsos

# The teacher's own Plickers exports, used to design and smoke the correction
# import. Real classes, real children — never commit. The fictitious fixtures
# the tests run against live in tests/Fixtures/Import/ and are versioned.
/Grelhas de correção

# Deploy package built locally per docs/deployment.md — never commit.
/update.tgz
# Written by `php artisan lapis:build-stamp` just before packaging, and read back
# in production by `lapis:release-check`. Committing it would pin a commit hash
# that describes the state before itself, so it is always generated, never stored.
/build.json

# Local AI-workflow state (never committed)
/.gstack/
/tmp/
